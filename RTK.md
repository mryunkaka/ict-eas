# RTK - Rust Token Killer (Project Instructions)

Gunakan `rtk` untuk semua command shell agar output yang masuk ke konteks lebih ringkas.

Contoh:

```bash
rtk git status
rtk rg "pattern" .
rtk read resources/js/app.js
rtk npm run build
```

Verifikasi:

```bash
rtk --version
rtk gain
```
